nomes = ['Flamengo', 'São Paulo', 'Corinthians', 'Botafogo', 'Fluminense', 'Vasco', 'Atlético-MG', 'Atlético-PR', 'Bahia', 'Cruzeiro', 'Fortaleza', 'Bragantino', 'Internacional', 'Juventude', 'Criciíuma']

letra = input("Qual a letra desejada? ").upper()

if letra == 'A':
    filtrados = [nome for nome in nomes if nome.startswith('A')]
    if filtrados:
        print(f"Palavras que começam com 'A': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'A'.")
elif letra == 'B':
    filtrados = [nome for nome in nomes if nome.startswith('B')]
    if filtrados:
        print(f"Palavras que começam com 'B': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'B'.")
elif letra == 'C':
    filtrados = [nome for nome in nomes if nome.startswith('C')]
    if filtrados:
        print(f"Palavras que começam com 'C': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'C'.")
elif letra == 'D':
    filtrados = [nome for nome in nomes if nome.startswith('D')]
    if filtrados:
        print(f"Palavras que começam com 'D': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'D'.")
elif letra == 'E':
    filtrados = [nome for nome in nomes if nome.startswith('E')]
    if filtrados:
        print(f"Palavras que começam com 'E': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'E'.")
elif letra == 'F':
    filtrados = [nome for nome in nomes if nome.startswith('F')]
    if filtrados:
        print(f"Palavras que começam com 'F': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'F'.")
elif letra == 'G':
    filtrados = [nome for nome in nomes if nome.startswith('G')]
    if filtrados:
        print(f"Palavras que começam com 'G': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'G'.")
elif letra == 'H':
    filtrados = [nome for nome in nomes if nome.startswith('H')]
    if filtrados:
        print(f"Palavras que começam com 'H': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'H'.")
elif letra == 'I':
    filtrados = [nome for nome in nomes if nome.startswith('I')]
    if filtrados:
        print(f"Palavras que começam com 'I': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'I'.")
elif letra == 'J':
    filtrados = [nome for nome in nomes if nome.startswith('J')]
    if filtrados:
        print(f"Palavras que começam com 'J': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'J'.")
elif letra == 'K':
    filtrados = [nome for nome in nomes if nome.startswith('K')]
    if filtrados:
        print(f"Palavras que começam com 'K': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'K'.")
elif letra == 'L':
    filtrados = [nome for nome in nomes if nome.startswith('L')]
    if filtrados:
        print(f"Palavras que começam com 'L': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'L'.")
elif letra == 'M':
    filtrados = [nome for nome in nomes if nome.startswith('M')]
    if filtrados:
        print(f"Palavras que começam com 'M': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'M'.")
elif letra == 'N':
    filtrados = [nome for nome in nomes if nome.startswith('N')]
    if filtrados:
        print(f"Palavras que começam com 'N': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'N'.")
elif letra == 'O':
    filtrados = [nome for nome in nomes if nome.startswith('O')]
    if filtrados:
        print(f"Palavras que começam com 'O': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'O'.")
elif letra == 'P':
    filtrados = [nome for nome in nomes if nome.startswith('P')]
    if filtrados:
        print(f"Palavras que começam com 'P': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'P'.")
elif letra == 'Q':
    filtrados = [nome for nome in nomes if nome.startswith('Q')]
    if filtrados:
        print(f"Palavras que começam com 'Q': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'Q'.")
elif letra == 'R':
    filtrados = [nome for nome in nomes if nome.startswith('R')]
    if filtrados:
        print(f"Palavras que começam com 'R': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'R'.")
elif letra == 'S':
    filtrados = [nome for nome in nomes if nome.startswith('S')]
    if filtrados:
        print(f"Palavras que começam com 'S': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'S'.")
elif letra == 'T':
    filtrados = [nome for nome in nomes if nome.startswith('T')]
    if filtrados:
        print(f"Palavras que começam com 'T': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'T'.")
elif letra == 'U':
    filtrados = [nome for nome in nomes if nome.startswith('U')]
    if filtrados:
        print(f"Palavras que começam com 'U': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'U'.")
elif letra == 'V':
    filtrados = [nome for nome in nomes if nome.startswith('V')]
    if filtrados:
        print(f"Palavras que começam com 'V': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'V'.")
elif letra == 'W':
    filtrados = [nome for nome in nomes if nome.startswith('W')]
    if filtrados:
        print(f"Palavras que começam com 'W': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'W'.")
elif letra == 'X':
    filtrados = [nome for nome in nomes if nome.startswith('X')]
    if filtrados:
        print(f"Palavras que começam com 'X': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'X'.")
elif letra == 'Y':
    filtrados = [nome for nome in nomes if nome.startswith('Y')]
    if filtrados:
        print(f"Palavras que começam com 'Y': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'Y'.")
elif letra == 'Z':
    filtrados = [nome for nome in nomes if nome.startswith('Z')]
    if filtrados:
        print(f"Palavras que começam com 'Z': {filtrados}")
    else:
        print("Nenhuma palavra encontrada que comece com 'Z'.")
else:
    print("Por favor, digite uma letra válida.")
